<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>
            blog - {% block title %}{% endblock %}
        </title>
        <link rel="stylesheet" href="/assets/css/bootstrap.min.css">
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path("articles") }}">
                    Blog
                </a>
                <div class="collapse navbar-collapse" id="navbarColor01">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item">
                            <a class="nav-link active" href="{{ path("articles") }}">
                                Accueil
                            </a>
                        </li>
                    </ul>
                </div>
                {% if app.user %}
                    <a href="{{ path("security_logout") }}" class="btn btn-danger">
                        Déconnexion
                    </a>
                {% else %}
                    <a href="{{ path("security_register") }}" class="btn btn-info mx-2">
                        Inscription
                    </a>
                    <a href="{{ path("security_login") }}" class="btn btn-success mx-2">
                        Connexion
                    </a>
                {% endif %}
            </div>
        </nav>
        <div class="container my-5">
            {% block body %}{% endblock %}
        </div>
        {% block javascripts %}{% endblock %}
    </body>
</html>
